result-view {
  match {
    State(state)
  }

  message {
    template ("[#{value(state.lastOption.onSelect.say)} ]#{value(state.currentScene.textToDisplay)}") {
      speech ("[#{value(state.lastOption.onSelect.say)} ]#{value(state.currentScene.textToSpeak)}")
    }
  }
  render {
   
  }
}